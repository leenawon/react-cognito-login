// React
import { BrowserRouter } from "react-router-dom";

// Router
import Router from "./Router.tsx";

function App() {
  return (
    <BrowserRouter>
      <Router />
    </BrowserRouter>
  );
}

export default App;
